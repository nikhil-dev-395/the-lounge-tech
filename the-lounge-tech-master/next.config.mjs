/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["via.placeholder.com", "assets.aceternity.com"], // Add all image host domains here
  },
};

export default nextConfig;
